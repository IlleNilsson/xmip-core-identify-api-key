# xmip-core-identify-api-key

Identify by api-key: a key in a header or a query parameter, presented as its id and never as itself; a transport-layer claim, passed. A technology of [xmip-core-identify](https://github.com/IlleNilsson/xmip-core-identify).

## Toolchain

`rust-toolchain.toml` pins the toolchain for the whole estate. Do not change it
here.

## Verification

The included workflow is manual-only and calls the versioned shared workflow at
`IlleNilsson/.github@v1`.
